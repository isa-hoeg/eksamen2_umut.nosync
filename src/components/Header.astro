<header>
  <nav>
    <div class="nav-left">
      <a href="/" class="button-link">Velkommen</a>
      <a href="/opskrifter" class="button-link">Opskrifter</a>
    </div>

    <a href="/" class="logo">
      <img src="/imgs/logo.svg" alt="Logo" />
    </a>

    <div class="nav-right">
      <a href="/kalender" class="button-link">Rejseguides & Livsstil</a>
      <a href="/kalender" class="button-link search-link">
        Søg
        <svg class="search-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="#efe4dc" width="18" height="18">
          <path d="M10 2a8 8 0 015.29 13.71l4 4a1 1 0 01-1.42 1.42l-4-4A8 8 0 1110 2zm0 2a6 6 0 100 12 6 6 0 000-12z"></path>
        </svg>
      </a>
    </div>

    <div class="burger" onclick="toggleMenu(this)">
      <div class="line"></div>
      <div class="line"></div>
      <div class="line"></div>
    </div>
  </nav>
</header>

<style>
  header {
    background-color: var(--skrift, #233249);
    padding: 20px;
  }

  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    position: relative;
  }

  .nav-left,
  .nav-right {
    display: none;
  }

  .logo img {
    height: 40px;
  }

  .button-link {
    text-decoration: none;
    font-family: "Playfair Display";
    font-size: 20px;
    color: var(--baggrund);
    padding: 10px;
  }
  .button-link:hover {
    color: var(--brun);
  }

  /* === BURGER MENU === */
  .burger {
    display: block;
    width: 30px;
    height: 20px;
    cursor: pointer;
  }

  .burger .line {
    width: 100%;
    height: 2px;
    background-color: var(--baggrund,);
    margin-bottom: 5px;
    transition: 0.2s linear;
  }

  /* === MOBILMENU (vises når .active) === */
  nav.active .nav-left,
  nav.active .nav-right {
    display: flex;
    flex-direction: column;
    width: 100%;
    margin-top: 20px;
  }

  nav.active .button-link {
    padding: 15px 20px;
    font-size: 18px;
  }
  .search-link {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .search-icon {
    width: 18px;
    height: 18px;
    object-fit: contain;
  }

  /* === DESKTOP: 850px og op === */
  @media (min-width: 1200px) {
    .burger {
      display: none;
    }

    .nav-left,
    .nav-right {
      display: flex;
      gap: 90px;
      margin-left: 40px;
      margin-right: 40px;
    }

    .logo img {
      height: 80px;
    }
  }
</style>
