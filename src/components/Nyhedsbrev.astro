
<body>
  <section class="nyhedsbrev">
    <link rel="stylesheet" href="/css/generel.css" />
    <div class="ramme">
      <div class="desktop">
        <h2>Få det bedste fra Umut – direkte til din inbox!</h2>
        <img src="/imgs/umut2.webp" alt="stregtegning af Umut" />
      </div>
      <div class="input-desktop">
        <input type="text" placeholder="Dit navn..." />
        <input type="email" placeholder="Email... Larslarsen@gmail.com" />
      </div>
      <button>Tilmeld mig!</button>
    </div>
  </section>

  <style>
    .ramme {
      text-align: center;
      width: 50%;
      margin: 0 auto;
    }

    h2 {
      font-family: "Playfair Display", serif;
      color: var(--skrift);
      margin: 20px 0;
      font-weight: lighter;
      font-size: 22px;
    }

    .ramme img {
      width: 50%;
      height: auto;
      margin-bottom: 10px;
    }

    .ramme input {
      width: 100%;
      padding: 6px 10px;
      font-size: 13px;
      font-family: "Playfair Display", serif;
      color: var(--skrift);
      border: 2px solid var(--brun);
      border-radius: 8px;
      background-color: var(--brun);
      margin-bottom: 10px;
    }

    .ramme button {
      display: block;
      margin: 10px auto 20px;
      padding: 8px 20px;
      font-size: 13px;
      font-family: "Playfair Display", serif;
      color: var(--baggrund);
      background-color: var(--skrift);
      border: 2px solid var(--skrift);
      border-radius: 8px;
      cursor: pointer;
    }

    .ramme button:hover {
      border: 4px solid var(--brun);
    }

    /* DESKTOP FORMAT */
    @media (min-width: 850px) {
      .input-desktop,
      .desktop {
        display: flex;
        justify-content: center;
        gap: 20px;
      }

      h2 {
        font-size: 30px;
        margin-right: 70px;
        margin-top: 90px;
      }

      .ramme img {
        width: 40%;
        margin-right: 80px;
      }

      .ramme button {
        padding: 10px 30px;
        font-size: 15px;
      }
    }
  </style>
</body>
