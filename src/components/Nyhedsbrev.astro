<body>
  <section class="nyhedsbrev">
    <div class="ramme">
      <div class="desktop">
        <h2>Få det bedste fra Umut -direkte til din inbox!</h2>
        <img src="/imgs/umut-streg.svg" alt="stregtegning af Umut" />
      </div>
      <div class="input-desktop">
        <input type="text" placeholder="Dit navn..." />
        <input type="email" placeholder="Email.... Larslarsen@gmail.com" />
      </div>
      <button>Tilmeld mig!</button>
    </div>
  </section>
</body>

<style>
  /* MOBIL FORMAT */
  .nyhedsbrev {
    display: flex;
    justify-content: center;
  }

  .ramme {
    text-align: center;
  }

  h2 {
    font-family: playfair display;
    color: var(--skrift);
    margin-bottom: 20px;
    margin-top: 20px;
    font-weight: lighter;
  }
  .ramme img {
    width: 50%;
    height: auto;
    margin-bottom: 10px;
    opacity: 0.6;
  }
  .ramme input {
    width: 100%;
    padding: 7px 10px;
    font-size: 13px;
    font-family: playfair display;
    color: var(--skrift);
    border: 2px solid var(--brun);
    border-radius: 8px;
    background-color: var(--brun);
    margin-bottom: 10px;
  }
  .ramme button {
    /* width: 100%; */
    display: block;
    margin-bottom: 20px;
    margin-top: 10px;
    padding: 8px 20px;
    font-size: 13px;
    font-family: playfair display;
    color: var(--baggrund);
    background-color: var(--skrift);
    border: 2px solid var(--skrift);
    border-radius: 8px;
    cursor: pointer;
  }
  .ramme button:hover {
    border: 4px solid var(--brun);
  }
  /* DESKTOP FORMAT */
  @media (min-width: 850px) {
    .input-desktop {
      display: flex;
      justify-content: center;
      gap: 20px;
    }
    .desktop {
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    h2 {
      font-size: 30px;
      margin-top: 90px;
      margin-left: 20px;
      font-family: playfair display;
      color: var(--skrift);
    }
    .ramme img {
      /* width: 50%;
      height: auto; */
      margin-top: 20px;
      opacity: 0.6;
      margin-right: 50px;
    }
    .ramme button {
      /* width: 100%; */
      display: block;
      margin-bottom: 20px;
      margin-top: 10px;
      padding: 10px 30px;
      font-size: 15px;
      font-family: playfair display;
      color: var(--baggrund);
      background-color: var(--skrift);
      border: 2px solid var(--skrift);
      border-radius: 8px;
      cursor: pointer;
    }
  }
</style>
